# 2026-04-26 — Session log

## Session 1 — Phase 0 fold-in, v1 site polish, design patterns

### Goal
Start with option B (fold Vic-Hako research into ROADMAP.md Phase 0) and option A (tagline pick + DNS for tonight's Vercel deploy). Mid-session, scope expanded to: deploy the production logo (nav + favicon), shuffle the deploy layout into `public/` per Phase 1 conventions, bump logo size after on-deploy review, and apply three design patterns from dyeconverter.com (eyebrow tags, split-clause headings, pillar sequencing) — captured as locked conventions in `tpt-site-build/SKILL.md`. Net: Phase 0 legal scaffolding documented; v1 launch site visually upgraded and ready to deploy.

### Deliverables

**Phase 0 roadmap**
- `ROADMAP.md` Phase 0 updated: 7 new milestones (Texas LLC formation [NOT PLLC], E&O + cyber liability insurance bound on the LLC, CDC Certified Divorce Coach × 2 parallel enrollment [buddy + Victor], High Conflict Institute foundational training, engagement-letter template per `tpt-upl-check` skill, FTC 2024 Consumer Review Rule clause). Attorney-advisor line refined with "Texas" + owner. Exit criteria expanded. Contingency note on engagement-letter + FTC items inline.

**Tagline + DNS (delivered to chat, awaiting Victor calls)**
- Tagline recommendation: SOUL.md candidate #4 *"For the dad new to family court. For the kids he's there for."* OR proposed fifth *"A steady path through family court — for the dad, for the kids."* Awaiting Victor approval before any change to SOUL.md or `index.html`.
- DNS records table for Vercel: Apex A → `76.76.21.21`; www CNAME → `cname.vercel-dns.com`. Includes registrar field-name translation map (Namecheap/GoDaddy/Cloudflare/Google) and gotchas (delete conflicting records, DNS-only on Cloudflare, apex as primary).

**`public/index.html` — site polish**
- Nav: inline SVG + wordmark `<span>` replaced with single `<img src="/assets/logo/logo.png" alt="The Primary Trail">` lockup. CSS cleanup of unused `.brand-mark` text styles.
- Favicon: inline SVG data URI → `/assets/logo/logo.png` (stopgap; square mark-only favicon added to open threads).
- Logo size: `.brand-logo` height 44px → 68px desktop, 48px mobile (new `@media` rule). Nav padding 1.5rem → 2rem (option B — true breathing room above the larger lockup).
- Eyebrow tags above 4 H2s (Pillars / Offerings / Who / FAQ) using new `.eyebrow` class (`--tpt-warm-gray`, 0.15em letter-spacing, 0.75rem gap to H2). Texts: "THE FRAMEWORK", "OFFERINGS", "WHO WE'RE FOR", "QUESTIONS".
- Split-clause headings on 3 H2s using new `.heading-accent` class (amber italic). "Who this is for." kept whole (too short to split).
- Pillar redesign: replaced single `.pillar-num` ("01 — CAPTURE") with stacked `.pillar-number` (big amber serif chapter number) + `.pillar-label` (small amber uppercase). Added `.pillar-grid::before` connecting line at low opacity (horizontal desktop → vertical mobile). Tightened `.pillar` padding from `2rem 1.75rem` → `1.5rem` so the four cards read as one grouped unit.
- Visible "Last updated" bumped from April 25 → April 26, 2026.
- Organization JSON-LD: added `"logo": "https://theprimarytrail.com/assets/logo/logo.png"`.
- WebSite JSON-LD: added `"dateModified": "2026-04-26"`.

**File-system hygiene**
- File shuffle: `index.html`, `robots.txt`, `llms.txt`, `sitemap.xml`, `vercel.json` moved from project root into `public/`. Project root retains `CLAUDE.md`, `ROADMAP.md`, `README.md`, `.gitignore`, `context/`, `.claude/`.
- 4 stray logo files at `public/` root deleted (`logo.png`, `logo.svg`, `logo-dark.png`, `logo-dark.svg`). Canonical lives at `public/assets/logo/logo.png` per `tpt-site-build/SKILL.md`.

**Skills + learnings**
- `.claude/skills/tpt-site-build/SKILL.md`: new "Design patterns (locked 2026-04-26)" section documenting eyebrow tags, split-clause headings, and pillar sequencing as locked conventions inherited by all future page builds.
- `context/learnings.md` (tpt-site-build section): new entry "Phase 1 deploy treats `public/` as the publish root — keep all customer-facing files inside it." Includes future-migration note re `vercel.json` location.

**Audit posture**
- 3 full AEO + UPL audit passes during the day (after each major change). Final state: PASS on both gates.
- AEO Flags resolved this session: A (`dateModified` in schema), B (`Organization.logo` in schema), E (stray `public/` files).
- AEO Flags still open: C (H2s are statements, not questions), D (uncited "$300–$500/hr" lawyer claim). Both routed to open threads with bundling note.

### Decisions
- **CDC credential** = parallel enrollment (buddy + Victor) for operator coverage. Buddy: enrolled + module 1 by 2026-05-23. Victor: enrolled by 2026-05-23, completion targeted Phase 1.
- **HCI training** stays buddy-primary, Victor-optional.
- **Engagement-letter + FTC clause** carry explicit contingency: if attorney advisor slips past 2026-05-16, both items slip to Phase 1 review week. Flagged inline in ROADMAP, not hidden.
- **Vercel apex** (theprimarytrail.com) is the primary domain; www redirects to apex.
- **Phase 1 deploy structure**: `public/` is the publish root for static drag-and-drop deploys; matches Phase 2 Next.js conventions. For tonight's drag-and-drop, `vercel.json` lives inside `public/` so it's at the deploy root.
- **Future Vercel migration**: when switching to Git-connected deploy with Output Directory = `public`, `vercel.json` moves back to project root (Vercel reads it from project root regardless of Output Directory). Documented in `learnings.md`.
- **Logo size**: 68px desktop / 48px mobile. Nav padding 1.5rem → 2rem (option B from the offered spectrum).
- **Favicon stopgap**: full horizontal lockup will smear at 16–32px tab size; replacement (square mark-only, ~64–128px) deferred to open threads.
- **Three design patterns adopted as page-level conventions** (eyebrow tags, split-clause headings + `.heading-accent`, pillar sequencing). Locked in `tpt-site-build/SKILL.md`. Future pages inherit; departures need a reason and Victor's call.
- **Cleanup bundling**: rather than splitting the date/JSON-LD/file-deletion cleanup into its own save, bundled with the design-patterns diff as a single approve+save cycle. Resolved AEO Flags A, B, E in one stroke.

### Open threads (for next session)
- **Build tpt-waitlist form in GHL + paste embed into `index.html`.** Replaces the TODO-commented native form placeholder. (Carried from 2026-04-25; deferred again tonight because GHL form not built yet.)
- **Generate square mark-only favicon (64×64 or 128×128).** Crop the mountain+trail glyph from the horizontal lockup, drop the wordmark, save as `public/assets/logo/favicon.png` (or `.ico`). Then update the `<link rel="icon">` in `index.html`. Horizontal lockup is in place tonight as a stopgap; it will look smeared at 16–32px browser-tab size.
- **Tagline approval.** Victor to choose between SOUL.md #4 and proposed fifth. Once approved, update SOUL.md (mark winner, retire others or move to "rejected candidates" footnote) and update index.html hero/meta/og:title.
- **Vercel deploy.** Drag-and-drop the contents of `public/` tonight; add custom domain in Vercel dashboard; verify DNS records match the table delivered today; confirm SSL issues clean. Then update Vercel project Settings → Build & Output → Output Directory = `public`.
- **Tagline → site copy propagation.** After tagline approval and Vercel deploy: confirm tagline appears in `<title>`, meta description, og:title, hero. Run `tpt-aeo-seo` audit on the live URL.
- **H2 question reframes (AEO Layer 4 fix).** All five page H2s are statements; AEO non-negotiable wants them as real questions. Examples: *"The four pillars: Capture, Organize, Reflect, Execute."* → *"What are the four pillars of The Primary Trail framework?"*; *"Who this is for."* → *"Who is The Primary Trail for?"*; *"What's coming, and what's available now."* → *"What does The Primary Trail offer?"* Natural bundling moment: when we touch the page for tagline approval (we'll already be in `<title>`, hero, and meta).
- **External citation for "$300–$500 an hour" lawyer claim (AEO Layer 4 + 5 fix).** Lines ~519 and ~598 in `public/index.html` cite this rate without an external source. Add a citation (ABA attorney-fee surveys, Clio Legal Trends Report, or similar). Bundle with H2 rework + tagline approval pass.
- **`vercel.json` migration heads-up.** When moving from tonight's drag-and-drop to a Git-connected Vercel deploy with Output Directory = `public`, relocate `vercel.json` from `public/` back to project root. Otherwise the `Content-Type: text/plain` header for `/llms.txt` and the `cleanUrls` config silently stop applying.

### Notes for future sessions
- **Engagement-letter + FTC-clause language**: customer-facing artifacts. Must run through `tpt-upl-check` at draft time. The skill's `references/` files (`engagement-letter.md`, `dtpa-line.md`, `upl-line.md`, `ofw-rule.md`) are scaffolded but still TBD — may need to populate them before the Phase 0 attorney-review pass.
- **Vercel DNS values can change** over time; always verify against the in-dashboard instructions Vercel shows after adding the custom domain — those are authoritative over our static table.
- **New skill: `tpt-customer-research`** added by a concurrent Cowork-Claude session. Triggers on "customer research", "dad interviews", "interview prep", "interview guide", "synthesize interviews". The Phase 0 8–12 dad-conversations milestone (due 2026-05-16) now routes through this skill — read it before drafting the recruitment plan or interview guide. Has its own section in `learnings.md`.
- **Design patterns are now locked conventions** in `tpt-site-build/SKILL.md` (eyebrow tags, split-clause headings + `.heading-accent`, pillar sequencing). Future page builds inherit them. Departures need a reason and Victor's call.
- **AEO Flags C and D bundle**: H2 question reframes + "$300–$500/hr" citation. Natural pairing with tagline approval pass; flagging here so the next session bundles all three rather than touching the page three times.
- **Cowork-Claude active in this project today**: expect git status to show changes I didn't make (new `tpt-customer-research/SKILL.md`, README registration, learnings section additions). Not a corruption signal — just concurrent work.
- **Workspace layout reminder**: project files live in nested `the-primary-trail/the-primary-trail/`, not at the working-dir root. Already captured in auto-memory.
