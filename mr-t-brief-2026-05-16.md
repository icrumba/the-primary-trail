# Mr T — Developer Brief

**Project:** The Primary Trail (theprimarytrail.com)
**From:** Victor Cuevas (operator)
**Written:** 2026-05-16
**Scope:** Four open implementation items on `public/index.html`. Pick them up in any order as upstream decisions unblock each.

> **Note:** an earlier draft of this file briefed a hero copy update — disregard that. The Lombardi epigraph hero shipped on 2026-05-15 (commit `e7309d6`) and is already live. No hero changes are open right now.

---

## Project context (quick orient)

The Primary Trail is a coaching and education service for dads new to family court. We are **not lawyers**. The Phase 1 site is a single static page at `public/index.html` deployed on Vercel. The repo follows an "Agentic OS" layout (`CLAUDE.md` is the operating manual at root; `context/` holds brand identity + memory; `.claude/skills/` holds workflow skills).

For deeper context: read `CLAUDE.md`, then `context/SOUL.md` (brand non-negotiables), then `session-catchup-2026-05-16.md` at repo root.

---

## Brand non-negotiables — check your work against these before pushing

Three gates apply to anything that touches customer-facing copy or markup. If anything fails or is unclear, ping Victor before shipping.

1. **UPL (Unauthorized Practice of Law) discipline.** We coach, educate, organize — we never advise on specific legal matters. Don't introduce copy that sounds like legal advice or implies outcome guarantees.
2. **Child-best-interest framing.** Everything orients around helping the dad be an effective advocate for his kids. Not "win against mom." Not "dad's rights." Always the children.
3. **Crisis-appropriate voice.** Audience is in active emotional crisis. No hype, urgency tactics, scarcity tricks, bro humor. The 11pm test: would this read okay at 11pm to a dad who just got served?

**Voice vocabulary —** *use:* path, framework, plan, prepared, organized, steady, evidence, record, communication, documentation, strategy, prep, the trail, walk it, the kids. *Avoid:* crush, win, beat, fight, battle, weapon, hack, secret, exposed, alpha, dad-rights, deadbeat, urgency clichés.

---

## The four open items

Each item has a STATUS label: **READY** (you can start now), **BLOCKED** (waiting on something from Victor). Read all four before starting so the order makes sense.

---

### Item 1 — Square mark-only favicon

**STATUS:** READY (or BLOCKED depending on path — see below).

**What.** The current favicon points at the full horizontal lockup (`/assets/logo/logo.png`, 600×337). At 16–32px browser-tab size, the wordmark smears and the mark is unreadable. We need a square mark-only version.

**Why.** A clean favicon is a credibility signal. Right now it's the weakest visual touchpoint on the brand.

**Two paths — Victor's choice:**

- **Path A** (you crop the existing logo). Crop the mountain-and-trail glyph out of the left side of `/assets/logo/logo.png`. Save as a clean square at multiple sizes. **READY** — you can do this now.
- **Path B** (designer produces a new square mark). Victor commissions a fresh square version from whoever made the original lockup. **BLOCKED** until that asset is delivered.

**Spec (either path):**

- Save the square mark to `public/assets/logo/`:
  - `favicon.ico` — multi-resolution (16, 32, 48 inside the .ico)
  - `favicon-32x32.png`
  - `favicon-16x16.png`
  - `apple-touch-icon.png` (180×180)
  - `favicon.svg` (optional but nice — modern browsers prefer it)
- Update the `<link rel="icon">` block in `public/index.html` (currently line 20) to reference the new files. Modern best-practice pattern:

```html
<link rel="icon" href="/assets/logo/favicon.ico" sizes="any">
<link rel="icon" href="/assets/logo/favicon.svg" type="image/svg+xml">
<link rel="icon" type="image/png" sizes="32x32" href="/assets/logo/favicon-32x32.png">
<link rel="icon" type="image/png" sizes="16x16" href="/assets/logo/favicon-16x16.png">
<link rel="apple-touch-icon" sizes="180x180" href="/assets/logo/apple-touch-icon.png">
```

- **Asset-organization rule:** anything new lives at `public/assets/<type>/<file>`. Never flat in `public/` root. (This is a locked convention — see `context/learnings.md`.)

**Acceptance:** load the site in Chrome and Safari at desktop and mobile, plus pin a tab — the mark reads cleanly at every size. No smearing.

---

### Item 2 — External citation for the "$300–$500 an hour" lawyer claim

**STATUS:** BLOCKED on Victor choosing a source.

**What.** Two places on the page reference lawyer hourly rates of $300–$500. Both are currently uncited.

**Where.** Search `public/index.html` for `$300` — should return two matches:

- The pillar #2 (Organize) card description: *"Lawyers charge $300 to $500 an hour to do this."*
- The Quick Answer / "What dads ask" area (FAQ section).

**Why.** AEO/SEO audit flagged this as an uncited claim. For credibility and AEO scoring, we need a linked external source.

**Blocked on Victor:**

- Pick a source. Two recommended:
  - **Clio Legal Trends Report** (annual; well-known in legal-tech circles; publicly available PDF)
  - **ABA (American Bar Association) attorney fee survey**
- Victor will deliver the URL.

**Spec (when unblocked):**

- Add a parenthetical citation at the first occurrence (pillar #2 card), and either repeat or cross-reference at the FAQ occurrence.
- Suggested pattern (inline, light-touch):

```html
Lawyers charge $300 to $500 an hour to do this (<a href="[URL]" target="_blank" rel="noopener">source</a>).
```

Or, more editorial:

```html
According to <a href="[URL]" target="_blank" rel="noopener">Clio's Legal Trends Report</a>, family-law attorneys charge $300 to $500 an hour for this kind of organization work.
```

- Style: keep the existing `.pillar p` color and weight; link uses the default `a` color (inherits to `--tpt-green`).

**Acceptance:** both occurrences are cited (or the second cross-references the first). Link opens in a new tab. No layout shift.

---

### Item 3 — H2 question reframes

**STATUS:** BLOCKED on Victor approving the new question wording.

**What.** Several page H2s are currently statements. AEO best-practice wants H2s to read as actual questions (so answer-engines can parse the page as a Q&A structure).

**Why.** AEO Layer 4 flagged this. Question H2s also help search snippets match user queries directly.

**Current H2s + draft reframes** (these are drafts — Victor has final call on wording):

| Current (statement) | Draft reframe (question) |
|---|---|
| The four pillars: Capture, Organize, Reflect, Execute. | What are the four pillars of The Primary Trail framework? |
| What's coming, and what's available now. | What does The Primary Trail offer? |
| Who this is for. | Who is The Primary Trail for? |
| What dads ask. | *(already question-shaped — leave or restyle to consistency)* |

**Blocked on Victor:** approval of the final question wording for each H2 (or replacement text he prefers).

**Spec (when unblocked):**

- Update the H2 text only — do NOT restructure the section, change classes, or touch the `.heading-accent` span pattern (the amber italic split-clause treatment must be preserved where it exists).
- After updating H2s, also update the FAQPage JSON-LD at the top of the file if any question mapping shifts. (The current JSON-LD has its own Q&A entries — verify they still match the FAQ section after edits.)

**Acceptance:** all H2s read as questions or remain intentionally as-is per Victor's call. JSON-LD validates (use https://search.google.com/test/rich-results to verify). Visual rhythm of the page unchanged.

---

### Item 4 — GoHighLevel waitlist form embed

**STATUS:** BLOCKED on the GHL form being built.

**What.** Two `<form class="waitlist">` blocks on the page (hero + CTA band, identified by IDs `waitlist-hero` and `waitlist-bottom`) are currently native HTML placeholders with a JavaScript interception that just fakes the success state. They need to be replaced with the real GoHighLevel form embed.

**Why.** No real lead capture today. The free "First 7 Things to Do" checklist promise depends on this working end-to-end (form submit → GHL contact created → welcome email + lead magnet delivered).

**Blocked on Victor:** Victor builds the GHL form. When ready, he'll deliver the GHL embed snippet (iframe or script tag from GHL's "Share" panel).

**Spec (when unblocked):**

- The GHL form must be tagged `tpt-waitlist` in GHL, with the welcome workflow that fires the "First 7 Things to Do" lead-magnet delivery.
- In `public/index.html`, replace the two existing `<form>` blocks (hero ~lines 633–637 and CTA band ~lines 791–795) with the GHL embed. Keep the surrounding `<div class="waitlist">` wrapper class on the parent if GHL allows it, so styling continues to apply — or restyle the embed to match.
- Preserve the helper microcopy under each form:
  - Hero: *"Get the free 'First 7 Things to Do' checklist when you join."*
  - CTA band: *"Free checklist included. We won't share your address."*
- Remove the `<script>` block at the bottom of `index.html` (the `wireForm` function + its two calls). That's placeholder logic only.
- Remove the corresponding `<div class="form-success">` blocks under each form (GHL handles its own success state).
- Verify the embed is mobile-responsive — both forms must render cleanly down to ~360px viewport width.
- Verify the GHL embed doesn't inject any unstyled fonts or rogue CSS. If GHL adds an iframe, set `loading="lazy"` and an appropriate height so there's no layout shift on initial render.

**Acceptance:** submitting either form creates a contact in GHL (Victor verifies), triggers the welcome email + lead-magnet delivery, and the user sees a sensible GHL-provided confirmation state inline. No browser console errors. Page weight increase ≤ 100KB.

---

## What NOT to touch

- The hero block (epigraph + H1 + lead). Already shipped 2026-05-15, brand-gated, and live. Do not modify.
- The Lora italic font import on the Google Fonts URL — that specifically supports the epigraph and must stay.
- JSON-LD `Organization`, `WebSite`, and `FAQPage` blocks unless an item above explicitly requires it (only Item 3 might).
- The CSS variable tokens (`--tpt-green`, `--tpt-amber`, etc.) and the existing `.heading-accent`, `.eyebrow`, `.pillar`, `.offer`, `.savings-quote` patterns. They're locked design conventions in `.claude/skills/tpt-design-system/`.
- The footer disclaimer text — it's been legally reviewed.
- The flat `public/` root — every static asset belongs in `public/assets/<type>/<file>`.

---

## Workflow + delivery

1. Branch off `main` for each item (or one branch for all four — your call).
2. Push to a Vercel preview deploy.
3. Send Victor the preview URL with a short note on what's in it.
4. He runs the brand gates and confirms.
5. Merge to `main` (auto-promotes on Vercel).
6. After merge, bump `dateModified` in two places: the visible footer "Last updated" line, AND the WebSite JSON-LD block near the top of `index.html`. Use the merge date.

If anything in this brief is unclear — especially around voice or the three non-negotiables — message Victor before guessing. We'd rather pause than ship something off-brand.

---

## TL;DR (for forwarding)

Four open items on `public/index.html`:

1. **Favicon** — square mark-only, replacing the smeared horizontal lockup. **READY** if you can crop the existing logo. (Item 1)
2. **$300–$500/hr citation** — add inline cite link at two locations. **BLOCKED** on Victor picking the source URL. (Item 2)
3. **H2 question reframes** — convert 3 statement-H2s into questions for AEO. **BLOCKED** on Victor approving wording. (Item 3)
4. **GHL waitlist form embed** — swap two placeholder native forms for the real GHL embed. **BLOCKED** on Victor building the GHL form. (Item 4)

Don't touch the hero (already shipped). Brand non-negotiables and "do not touch" list inside.
